import styled, { css } from 'styled-components'

import * as palette from '../../palette'

export const twoColumn = css`
  display: flex;
  
  .left {
    width: 70%;
  }

  .right {
    width: 30%;
  }
`

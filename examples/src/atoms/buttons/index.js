import React from 'react'
import styled from 'styled-components'

import * as style from './style'

export const Primary = styled.button`${style.primary}`

export const Remove = styled.button`${style.remove}`

export const Secondary = styled.button`${style.base}`
